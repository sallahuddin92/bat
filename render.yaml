# Render.com Build Configuration

# Build command (if needed)
buildCommand: npm install

# Start command
startCommand: npm start

# Health check path
healthCheckPath: /

# Environment
env: node

# Region (optional)
region: singapore
